<h1>商品一覧</h1>

<table class="table">
  <tbody>
     <% @items.each do |item| %>
     <tr>
       <!--商品画像入れるコード-->
        <th><%= item.name %></th>
        <th>¥<%= item.price %></th>
      </tr>
     <% end %>
  </tbody>
</table>