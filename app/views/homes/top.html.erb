<div class="container">
  <div class="row">
    <div class="col-xs-12 mx-auto">
      <div class="first-page">
        <p>ようこそ、ながのCAKEへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。
        </p>
      </div>
    </div>
  </div>
</div>

<div class="container new-item">
  <h4>新着商品</h4>
  <div class="row mx-auto mt-3">
    <% @new_items.each do |new_item| %>
    <div class="mx-auto">
      <%= link_to public_item_path(new_item.id) do %>
      <%= attachment_image_tag new_item, :image, size: "150x100" %>
      <% end %>
      <p class="mt-3"><%= new_item.item_name %></p>
      <p>¥<%= new_item.price %></p>
    </div>
    <% end %>
  </div>
</div>